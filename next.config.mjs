/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
      // This will ignore ESLint warnings and errors during production builds
      ignoreDuringBuilds: true,
    },
  };
  
  export default nextConfig;
